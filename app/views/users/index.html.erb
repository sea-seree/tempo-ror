<div class="show-users">
  <section>
    <header>Users List
      <%= link_to "javascript:history.back()", class: 'btn-register' do %>
        <%= image_tag('plus-solid.svg', alt: 'plus', class: 'plus-icon') %>
        Create new account
      <% end %>
    </header>
    <div class="users-list">
      <% @users.each do |user| %>
        <div class="user-card">
          <div class="user-info">
            <strong>First Name:</strong> <%= user.first_name %><br>
            <strong>Last Name:</strong> <%= user.last_name %><br>
            <strong>Email:</strong> <%= user.email %>
          </div>
          <div class="user-actions">
            <%= link_to 'View', user, class: 'btn-view' %>
            <%= link_to 'Edit', edit_user_path(user), class: 'btn-edit' %>
            <%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn-delete' %>
          </div>
        </div>
      <% end %>
    </div>
  </section>
</div>
